feedparser>=6.0.10
# Capped at 2.27.1: requests dropped Python 3.7- support above this version, and the prod
# host (server357) is still on Python 3.6 as of 2026-08. Raise this floor once that host is
# upgraded -- there's no functional reason to stay pinned this low otherwise.
requests>=2.25.0,<=2.27.1
beautifulsoup4>=4.12.0
python-dateutil>=2.8.2
lxml>=4.9.0
# Capped below 1.0.0: python-dotenv 1.0.0 requires Python 3.8+, which the prod host doesn't
# have yet (see requests note above). Same caveat -- relax this once Python is upgraded there.
python-dotenv<1.0.0
